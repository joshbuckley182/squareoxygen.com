{
  "name": "squareoxygen.com",
  "version": "0.0.1",
  "scripts": {
    "sass-compile": "sass --update ./assets/sass:./assets/css --style=compressed",
    "sass-watch": "sass --watch ./assets/sass:./assets/css --style=compressed"
  },
  "dependencies": {
    
  }
}
